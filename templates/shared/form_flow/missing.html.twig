{#
    Final fallback when neither an entity-specific nor a generic operation template exists.
#}
<p>No template configured for the "{{ flowOperation|default('?') }}" operation of {{ flowModel|default('this resource') }}.</p>
